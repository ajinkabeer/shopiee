import React from "react";
import Lottie from "react-lottie";
import { defaultOptions } from "../../assets/defaultOptions";

const Animation = () => (
  <Lottie options={defaultOptions} height={100} width={100} />
);

export { Animation };
